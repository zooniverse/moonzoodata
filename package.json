{
  "name": "moonzoodata",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap-styl": "^4.0.5",
    "harp": "^0.17.0",
    "nib": "^1.1.0"
  },
  "scripts": {
    "run": "./node_modules/.bin/harp server",
    "deploy": "./node_modules/.bin/harp compile . build && rm -rf build"
  }
}
